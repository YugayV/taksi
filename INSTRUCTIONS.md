# 🚕 Taxi Uzbekistan - Полное Руководство

Это подробная инструкция по установке, настройке, запуску и развертыванию проекта **Taxi Uzbekistan**.

---

## 📋 1. Требования (Prerequisites)

Перед началом убедитесь, что у вас установлены:

1.  **Node.js** (версия 18 или выше). Скачать: [nodejs.org](https://nodejs.org/).
2.  **Git**. Скачать: [git-scm.com](https://git-scm.com/).
3.  **Expo Go** (на вашем телефоне) или **Android Emulator** (на компьютере).
4.  **PostgreSQL** (опционально, для локальной базы данных). Если нет, проект будет работать с "моковыми" (тестовыми) данными.

---

## 🛠 2. Установка (Installation)

### Шаг 1: Скачивание проекта
Если вы скачали архив, распакуйте его. Если используете Git:
```bash
git clone <url-репозитория>
cd taksi
```

### Шаг 2: Установка зависимостей
В проекте есть удобная команда для установки всех библиотек сразу:

```bash
npm run install:all
```

*Или вручную:*
```bash
cd backend
npm install
cd ../mobile
npm install
```

---

## ⚙️ 3. Настройка (Configuration)

### Backend (.env)
1.  Перейдите в папку `backend`.
2.  Создайте файл `.env` (скопируйте из `.env.example`):
    ```bash
    cp .env.example .env
    ```
3.  Откройте `.env` и заполните ключи:
    ```env
    PORT=3000
    # Если есть локальная база (не обязательно для теста):
    DATABASE_URL=postgresql://user:password@localhost:5432/taksi_db
    # Для работы AI (Gemini):
    GEMINI_API_KEY=ваш_ключ_от_google_ai_studio
    # Токен бота (для будущих функций):
    TELEGRAM_BOT_TOKEN=ваш_токен
    ```

### Mobile (API URL)
1.  Откройте файл `mobile/App.js`.
2.  Найдите строку `const API_URL = ...`.
3.  Выберите правильный адрес:
    -   Если запускаете на **Android Emulator**: `http://10.0.2.2:3000/api`
    -   Если запускаете на **Real Device (Телефон)**: `http://192.168.1.XX:3000/api` (замените `192.168.1.XX` на IP вашего компьютера).
    -   Если запускаете на **iOS Simulator**: `http://localhost:3000/api`

---

## 🚀 4. Запуск (Running)

Вам понадобятся **два** терминала.

### Терминал 1: Запуск Сервера (Backend)
```bash
npm run start:backend
```
Вы увидите сообщение: `Server is running on http://localhost:3000`.

### Терминал 2: Запуск Приложения (Mobile)
```bash
npm run start:mobile
```
Или для Android сразу:
```bash
npm run android
```

В терминале появится QR-код. Отсканируйте его через приложение **Expo Go** на телефоне (Android) или камеру (iOS).

---

## 🌍 5. Развертывание (Deployment)

### Backend (Railway)
1.  Зарегистрируйтесь на [Railway.app](https://railway.app/).
2.  Создайте "New Project" -> "Deploy from GitHub".
3.  Выберите репозиторий `taksi`.
4.  **ВАЖНО:** Перейдите в **Settings** -> **General** -> **Root Directory** и укажите `/backend`.
    *   *Если вы не сделаете это, Railway будет использовать скрипты из корня, которые мы настроили для совместимости, но лучше указать явную папку.*
5.  В разделе **Variables** добавьте:
    *   `GEMINI_API_KEY`: ваш ключ.
    *   `PORT`: `3000` (не обязательно, Railway обычно сам дает PORT, но лучше указать).
6.  Добавьте PostgreSQL Database через интерфейс Railway.


### Mobile (APK для Android)
1.  Установите EAS CLI: `npm install -g eas-cli`.
2.  Войдите в аккаунт Expo: `eas login`.
3.  Настройте проект: `eas build:configure`.
4.  Запустите сборку:
    ```bash
    npm run build:android
    ```
5.  После завершения вы получите ссылку на скачивание `.apk` файла.

---

## ❓ Частые проблемы (Troubleshooting)

**1. Ошибка "Network request failed" в приложении:**
*   **Причина:** Телефон не видит компьютер.
*   **Решение:** Убедитесь, что они в одной Wi-Fi сети. Проверьте `API_URL` в `mobile/App.js` (должен быть IP компьютера, не localhost). Отключите брандмауэр (Firewall) на ПК.

**2. Ошибка "Google Generative AI Error":**
*   **Причина:** Неверный или отсутствующий API Key.
*   **Решение:** Проверьте `GEMINI_API_KEY` в `backend/.env`.

**3. Ошибка при `npm install`:**
*   **Решение:** Попробуйте удалить `node_modules` и `package-lock.json`, затем запустите `npm install` снова. Используйте Node.js версии LTS.

---
**Разработано с помощью Trae AI**
